{
  "name": "@miketickets/common",
  "version": "1.0.1",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "license": "MIT",
  "scripts": {
    "clean": "del ./build/*",
    "build": "yarn clean && tsc",
    "pub": "git add . && git commit -m \"Update\" && npm version patch && yarn build && npm publish"
  },
  "devDependencies": {
    "del-cli": "^4.0.0",
    "typescript": "^4.3.5"
  }
}
